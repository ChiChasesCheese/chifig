# chifig v2 调研汇总（2026-09-06）

## 1. 仓库现状 ChiChasesCheese/chifig
- 公开仓库，2026-09-05 建，2 commits，1 branch(main)，本地 ~/chi_config 与远端零差异，remote 为 HTTPS。
- 内容：Brewfile(14 项) / install.sh(幂等：brew bundle→cp fonts→ln ghostty→append source 到 ~/.zshrc→git include.path) / ghostty/config / git/config / zsh/chi.zsh / fonts MesloLGS NF x4 / claude/cc-tips.sh + tips/ + HANDOFF.md
- 管理方式：自写 bash 软链+追加；README 明确不入库 ~/.claude、~/.zshrc 本体、API key、OMZ/p10k。
- 本机已生效接入点：~/.config/ghostty/config 软链；~/.zshrc 末行 source；git include.path；~/.claude/cc-tips.sh 软链；ccstatusline tip 组件。
- 既有记忆规则(~/.claude/projects/-Users-chizhang/memory/reference-chifig-dotfiles.md)：公共配置改 ~/chi_config 并 push；仓库公开，绝不放密钥/内网IP/用户名。

## 2. 本机现状（M4 Pro / macOS 26.6.2 / Homebrew 6.0.22 / zsh 5.9）
- 已装：fzf rg fd bat eza delta lazygit gh jq tmux yazi btop uv(pyenv shim) pyenv nvm pnpm deno rustup cargo go java docker kubectl gcloud ollama rtk autojump httpie
- 未装：zoxide atuin starship nvim mise direnv chezmoi stow age sops op(1Password) aerospace karabiner raycast bun fnm yq zellij tealdeer dust
- brew leaves 48 / casks 9(calibre chromedriver codex gcloud-cli ghostty mactex miniconda swiftformat-for-xcode vnc-viewer) / npm -g 9(ccstatusline claude-auto-retry openclaw repomix ...) / uv tools 2
- Shell：oh-my-zsh + powerlevel10k(~/.p10k.zsh 88K)；plugins=(git jsontools z zsh-syntax-highlighting zsh-autosuggestions autojump)；custom 插件手动 clone
- ~/.zshrc 问题：坏行 `echo ... >> ~/.zshrcexport PATH=...`(每次开 shell 追加，已 280 行)；明文 OPENAI_API_KEY；postgresql@14 PATH 不存在；PGDATA 重复；Windsurf 残留；Python.framework 3.8 在 .zprofile
- Python 6 来源：pyenv(默认 3.11.5, 3.12.9) / cask miniconda(conda init 源) / ~/miniconda3 / ~/miniforge3 / brew 3.12-3.14 / 系统 3.8 framework；uv 0.9.26 经 pyenv shim
- Node：brew node 26.8.1 默认 + nvm 22/23；Go 1.25.7；Rust 1.87；Java 26；Ruby brew
- 编辑器：Cursor 为实际主力(settings 1.7K)，VS Code 裸；20 个扩展两边相同；无 nvim/zed 配置。终端 Ghostty(主) + iTerm2 残留
- GUI：Alfred 5、Paste、Tiles、Docker Desktop、Obsidian、Claude/ChatGPT/Codex/Ollama app；无 Raycast/Rectangle/Karabiner/1Password
- Claude Code：~/.claude/{CLAUDE.md,RTK.md,settings.json}；无 hooks/ keybindings；10 插件；34 skills(32 软链 ~/.agents/skills)；MCP 全局 context7 playwright github obsidian
  - settings.json 问题：env 明文 gho_ token；autoMode.environment 塞了 SystemDesign 项目内容
  - hooks: PreToolUse(Bash)→rtk hook claude; StopFailure→claude-auto-retry; statusLine ccstatusline
- 其他 agent：~/.codex(config.toml, AGENTS.md 空) ~/.cursor(mcp.json hooks.json, skills 软链→~/.claude/skills) ~/.gemini(GEMINI.md 空)
- ~/.gitconfig 与 chi_config/git/config 内容重复；~/.config/git/ignore 只有 settings.local.json；~/.tmux.conf 仅 default-shell
- 敏感：~/.config/gws*/ credentials；~/.weixin_cow_credentials.json —— 新仓库必须排除
- 垃圾：.zshrcexport .zshrc.pre-oh-my-zsh .zprofile.pysave .zshDDPMenv 6 个 .zcompdump* .claude.json.backup *.bak

## 3. 趋势（star / 最后 push，2026-09-06 GitHub API）
- dotfiles 管理：chezmoi 21.5k/09-05 v2.72.1；yadm 6.4k/04-13；dotbot 8.0k；nix-darwin 5.9k + home-manager 10.3k；stow 无模板/secret
- zsh 插件管理：zinit 4.8k；zim 4.7k；antidote 1.8k（静态生成、纯文本清单）；oh-my-zsh 189.6k；zsh4humans 2.3k/05-01
- prompt：starship 59.8k/09-06；p10k 55.0k 2024-05 起 life support
- fish 4.6：agent(Claude Code/Codex) 生成 POSIX 命令，fish 独有 env 在 agent 子 shell 丢失 → 不切
- 插件：zsh-autosuggestions 36.0k(push 2025-06)；fast-syntax-highlighting 1.7k/08-31 优于 zsh-syntax-highlighting 23.0k；zsh-completions 7.9k；fzf-tab 4.9k。顺序 completions→fzf-tab→autosuggestions→syntax-highlighting(最后)
- CLI 必装：ripgrep 68k fd 44k bat 60k eza 23k zoxide 39k fzf 83k delta 32k atuin 31.5k jq 36k btop 34k tealdeer 6.5k hyperfine 29k yazi 42k
- CLI 可选：difftastic 26k dust 12k duf 15k(停滞) sd 7k xh 8k yq 16k procs 6k television 6k
- 终端：Ghostty 60.8k 1.3(2026-03)；kitty 35k WezTerm 29k Alacritty 66k iTerm2 18k；Warp 2026-04 AGPL 开源但核心 SaaS
- 多路复用：tmux 49k 3.7 浮动 pane；zellij 35k；远程只有 tmux → 统一 tmux
- 编辑器：Neovim 0.12 内置 vim.pack；kickstart 31k/09-06 最活跃；LazyVim 27k/06-02；Zed 89.8k 1.0(2026-04-29) 原生 ACP 跑 Claude Code；Helix 46k 无插件
- Cursor：settings.json/keybindings.json 软链到 ~/Library/Application Support/Cursor/User/；扩展用 Brewfile vscode 指令
- 运行时：mise 33.5k(无 shim，内置 env+task，替代 direnv)；asdf 25.6k；uv 89.5k；pixi 7.7k 仅 CUDA/R 场景；fnm 26.8k
- Git：lazygit 82k；gh 46k；difftastic 做 difftool + delta 做 pager；核心开发者配置(GitButler 2025-02)：column.ui branch.sort=-committerdate diff.algorithm=histogram colorMoved push.autoSetupRemote fetch.prune rerere rebase.autoStash/updateRefs merge.conflictstyle=zdiff3 help.autocorrect=prompt commit.verbose；includeIf 多身份；SSH 签名 gpg.format=ssh；git maintenance start
- Secrets：1Password CLI + chezmoi onepasswordRead；age 23k 兜底；sops 个人多余。~/.claude.json 含 session/MCP token 不入库
- macOS：defaults.sh 参考 macos-defaults.com（mathiasbynens .macos 2024-08 停更，Tahoe 大量失效）；Brewfile 支持 brew/cask/tap/mas/vscode/go/cargo/npm/uv；AeroSpace 22.9k(不关 SIP) vs yabai 29.6k(要关 SIP) vs Rectangle 29.8k；Raycast 取代 Alfred；Karabiner 22.8k karabiner.json 直接纳管
- AI 配置：~/.claude/{CLAUDE.md,settings.json,rules,skills,agents,commands,hooks} 进 chezmoi；排除 ~/.claude.json history credentials projects；AGENTS.md(AAIF 标准) 单源模板渲染 CLAUDE.md + ~/.codex/AGENTS.md；MCP token 走 settings.local.json 或 onepasswordRead
- 范例：twpayne/dotfiles(.chezmoiroot=home, .chezmoiexternal, .chezmoiscripts)；signalridge/dotfiles(dot_claude dot_codex dot_cursor dot_gemini + AGENTS.md + Justfile.tmpl + .chezmoidata + tests + gitleaks)；mizchi/chezmoi-dotfiles(极简)；holman/dotfiles(topic-centric)；br3ndonland/dotfiles(bootstrap.sh + CI)
- bootstrap：curl|sh 用 main(){..}; main "$@" 防截断，固定 ref；四步 CLT→brew→chezmoi→init --apply；run_once_*/run_onchange_*(Brewfile hash)；just 35.7k 做入口(apply/update/brew/test/lint)；CI macos-latest 跑 bootstrap + chezmoi verify + shellcheck + gitleaks；mas 用 unless ENV['CI']

## 4. 推荐栈
chezmoi | Brewfile | zsh+antidote+starship | f-s-h+autosuggestions+completions+fzf-tab | rg fd bat eza zoxide fzf delta atuin jq btop tealdeer hyperfine yazi | Ghostty+tmux | nvim(kickstart)+Zed+Cursor | mise+uv(+conda 仅科学) | git XDG config+includeIf+SSH sign+delta+lazygit+gh | 1Password(+age 兜底) | AeroSpace | Raycast+Karabiner | ~/.claude 进 chezmoi, AGENTS.md 单源 | just | GH Actions macos-latest

## 5. 不确定
p10k Tahoe 兼容未验；zsh-autosuggestions 2025-06 后无 push；LazyVim 与 nvim 0.12 兼容有 discussion；nix-darwin 2026 稳定性无明确记录；Claude Code hooks/skills 目录约定演进快，落地前对一次 code.claude.com/docs/en/settings
